<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.ly.ProIndetailDao">
	<select id="selProIn" resultType="map" parameterType="int">
		select p1.title,p1.goal,p1.projects_place,p1.financing,p1.financing_day,p1.cover from projects_describe p1
		where projectsid=#{projectsid}
	</select>
	<select id="selDescribe" resultType="com.entity.ly.ProDescribe" parameterType="com.entity.ly.ProDescribe">
		select p1.indetailid,p1.projectsid,pt.places,pt.title,pt.body 
		from projects_indetail p1,projects_indetail_text pt
		where p1.indetailid=pt.indetailid and projectsid=#{projectsid}
	</select>
</mapper>